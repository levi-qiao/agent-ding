# @agent-ding/notify

Standalone completion notifier.

```bash
# from repo root
./install.sh --only notify
agent-ding claude
```

Requires macOS + `terminal-notifier` for branded icons; Linux falls back to `notify-send`.
